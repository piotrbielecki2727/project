import Button from '@/components/ui/Button'

export default function Home() {
  return (
    <div>
      <Button
        onClick={() => ({})}
        variant='destructive'
        additionalButtonVariant='success'
      >
        aha
      </Button>
    </div>
  )
}
